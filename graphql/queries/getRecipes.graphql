query {
    recipesQuery(recipesRequest: {$option: String}) {
        recipes {
            id
            title
            description
            image_url
        }
        links {
            next
            prev
        }
    }
}
