query recipesQuery($recipesRequest: RecipeRequest) {
        recipes {
            id
            title
            description
            image_url
        }
        links {
            next
            prev
        }
}